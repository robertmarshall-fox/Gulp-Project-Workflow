var gulp        = require('gulp');
//var del         = require('del');

var bases       = require('../../gulpconfig').bases;
var paths       = require('../../gulpconfig').paths;

gulp.task('utils:wipe', function() {
    //return del( bases );
});